from PIL import Image
import base64
from io import BytesIO
# 获取图片的byte[]数据

# 将字节流解码为base64编码的字符串
# b64_str = base64.b64encode(bytes(a["bytes"])).decode()

# 将base64编码的字符串转换为PIL Image对象
# img = Image.open(BytesIO(base64.b64decode(b64_str)))

# 显示图片
# img.show()